

<div class="container">
  <div class="bg-img">
<h1 id="showh"><%= @cocktail.name %></h1>
<h2 id="showh">DOSES 🔥 :</h2>


 <p id="showh"><% @cocktail.doses.each do |dose| %>
 <%=  dose.ingredient.name  %>
 <%= dose.description %><br>

 <%end%>
 </p>

</div>
<%= link_to 'Add dose', new_cocktail_dose_path(@cocktail), class: 'btn btn-ghost'  %>
<%= link_to 'Back home', cocktails_path, class: 'btn btn-ghost'  %>
</div>
